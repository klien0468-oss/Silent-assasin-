--// Services
local Players = game:GetService("Players")
local RS = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local LP = Players.LocalPlayer
local Camera = workspace.CurrentCamera

--// States (Giữ nguyên gốc)
local AutoHit = false
local PlayerESP = false
local AutoBasic = false
local AutoDivine = false
local currentTab = "Main"

--// UI PREMIUM CYBERPUNK
local gui = Instance.new("ScreenGui", game:GetService("CoreGui"))
gui.Name = "LordGuyCyberpunk"
gui.ResetOnSpawn = false

-- Khung Main Menu Chính
local main = Instance.new("Frame", gui)
main.Name = "MainFrame"
main.Size = UDim2.fromOffset(420, 260)
main.Position = UDim2.new(0.5, -210, 0.5, -130)
main.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
main.BorderSizePixel = 0
main.ClipsDescendants = false -- Để avatar lồi lên trên

local mainCorner = Instance.new("UICorner", main)
mainCorner.CornerRadius = UDim.new(0, 16)

-- Viền Rainbow chuyển động cho Menu chính
local mainStroke = Instance.new("UIStroke", main)
mainStroke.Thickness = 3
local mainGradient = Instance.new("UIGradient", mainStroke)
mainGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 128)),
    ColorSequenceKeypoint.new(0.25, Color3.fromRGB(0, 255, 255)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 255, 128)),
    ColorSequenceKeypoint.new(0.75, Color3.fromRGB(255, 255, 0)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 128))
})

task.spawn(function()
    while true do
        for i = 0, 360, 2 do
            if not mainGradient then break end
            mainGradient.Rotation = i
            task.wait(0.01)
        end
    end
end)

-- Vùng kéo di chuyển Menu (Drag Zone)
local DragHeader = Instance.new("Frame", main)
DragHeader.Name = "DragHeader"
DragHeader.Size = UDim2.new(1, 0, 0, 45)
DragHeader.BackgroundTransparency = 1

-- Tiêu đề Menu
local title = Instance.new("TextLabel", main)
title.Size = UDim2.new(1, -120, 0, 45)
title.Position = UDim2.new(0, 15, 0, 0)
title.BackgroundTransparency = 1
title.Text = "⚡ TẬP LÀM SCPRIT HUB"
title.Font = Enum.Font.GothamBold
title.TextSize = 15
title.TextColor3 = Color3.new(1, 1, 1)
title.TextXAlignment = Enum.TextXAlignment.Left

-- [AVATAR GIỮA ĐỈNH MENU]
local AvatarFrame = Instance.new("Frame", main)
AvatarFrame.Name = "AvatarFrame"
AvatarFrame.Size = UDim2.fromOffset(55, 55)
AvatarFrame.Position = UDim2.new(0.5, -27.5, 0, -27.5)
AvatarFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
Instance.new("UICorner", AvatarFrame).CornerRadius = UDim.new(1, 0)
local avStroke = Instance.new("UIStroke", AvatarFrame)
avStroke.Thickness = 2
avStroke.Color = Color3.fromRGB(0, 255, 255)

local AvatarImage = Instance.new("ImageLabel", AvatarFrame)
AvatarImage.Size = UDim2.new(1, -6, 1, -6)
AvatarImage.Position = UDim2.new(0, 3, 0, 3)
AvatarImage.BackgroundTransparency = 1
AvatarImage.Image = "rbxassetid://73572444761388"
Instance.new("UICorner", AvatarImage).CornerRadius = UDim.new(1, 0)

-- Nút Tắt Menu (✕)
local close = Instance.new("TextButton", main)
close.Size = UDim2.fromOffset(30, 30)
close.Position = UDim2.new(1, -35, 0, 8)
close.BackgroundTransparency = 1
close.Text = "✕"
close.Font = Enum.Font.GothamBold
close.TextSize = 16
close.TextColor3 = Color3.fromRGB(255, 70, 70)

-- Nút Ảnh Tròn Mở Lại Ngoài Màn Hình (Mobile Toggle Button)
local OpenBtn = Instance.new("ImageButton", gui)
OpenBtn.Name = "OpenBtn"
OpenBtn.Size = UDim2.fromOffset(50, 50)
OpenBtn.Position = UDim2.new(0.05, 0, 0.2, 0)
OpenBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
OpenBtn.Image = "rbxassetid://73572444761388"
OpenBtn.Visible = false
Instance.new("UICorner", OpenBtn).CornerRadius = UDim.new(1, 0)
local opStroke = Instance.new("UIStroke", OpenBtn)
opStroke.Thickness = 2.5
opStroke.Color = Color3.fromRGB(0, 255, 150)

-- TABS CONTAINER
local TabsContainer = Instance.new("Frame", main)
TabsContainer.Size = UDim2.new(1, -30, 0, 35)
TabsContainer.Position = UDim2.new(0, 15, 0, 50)
TabsContainer.BackgroundTransparency = 1

-- Tạo Nút Tab Hiện Đại
local tab1 = Instance.new("TextButton", TabsContainer)
tab1.Size = UDim2.new(0.48, 0, 1, 0)
tab1.Position = UDim2.new(0, 0, 0, 0)
tab1.Text = "Chức Năng Chính"
tab1.Font = Enum.Font.GothamBold
tab1.TextSize = 13
tab1.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
tab1.TextColor3 = Color3.new(1, 1, 1)
Instance.new("UICorner", tab1).CornerRadius = UDim.new(0, 8)

local tab2 = Instance.new("TextButton", TabsContainer)
tab2.Size = UDim2.new(0.48, 0, 1, 0)
tab2.Position = UDim2.new(0.52, 0, 0, 0)
tab2.Text = "Mở Rương Auto"
tab2.Font = Enum.Font.GothamBold
tab2.TextSize = 13
tab2.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
tab2.TextColor3 = Color3.fromRGB(150, 150, 150)
Instance.new("UICorner", tab2).CornerRadius = UDim.new(0, 8)

-- PAGES CONTAINER
local page1 = Instance.new("Frame", main)
page1.Size = UDim2.new(1, -30, 1, -105)
page1.Position = UDim2.new(0, 15, 0, 95)
page1.BackgroundTransparency = 1

local page2 = page1:Clone()
page2.Parent = main
page2.Visible = false

-- HÀM TOGGLE CAO CẤP (NÚT ON/OFF KIỂU CYBERPUNK TỰ ĐỔI MÀU)
local function Toggle(text, parent, pos, callback, defaultState)
    local btn = Instance.new("TextButton", parent)
    btn.Size = UDim2.new(0, 185, 0, 42)
    btn.Position = pos
    btn.BackgroundColor3 = defaultState and Color3.fromRGB(0, 180, 90) or Color3.fromRGB(28, 28, 35)
    btn.Text = text .. " : " .. (defaultState and "ON" or "OFF")
    btn.Font = Enum.Font.GothamMedium
    btn.TextSize = 13
    btn.TextColor3 = Color3.new(1, 1, 1)
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)

    local state = defaultState
    btn.MouseButton1Click:Connect(function()
        state = not state
        btn.Text = text .. " : " .. (state and "ON" or "OFF")
        TweenService:Create(btn, TweenInfo.new(0.2), {
            BackgroundColor3 = state and Color3.fromRGB(0, 180, 90) or Color3.fromRGB(28, 28, 35)
        }):Play()
        callback(state)
    end)
end

-- Khởi tạo các Toggles (Đúng nguyên gốc vị trí và chức năng)
Toggle("Auto Hit All", page1, UDim2.new(0, 0, 0, 5), function(v) AutoHit = v end, AutoHit)
Toggle("Player ESP", page1, UDim2.new(0, 205, 0, 5), function(v) PlayerESP = v end, PlayerESP)

Toggle("Auto Basic Chest", page2, UDim2.new(0, 0, 0, 5), function(v) AutoBasic = v end, AutoBasic)
Toggle("Auto Divine Chest", page2, UDim2.new(0, 205, 0, 5), function(v) AutoDivine = v end, AutoDivine)

-- LOGIC CHUYỂN TAB MƯỢT MÀ
tab1.MouseButton1Click:Connect(function()
    currentTab = "Main"
    page1.Visible = true
    page2.Visible = false
    tab1.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
    tab1.TextColor3 = Color3.new(1, 1, 1)
    tab2.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    tab2.TextColor3 = Color3.fromRGB(150, 150, 150)
end)

tab2.MouseButton1Click:Connect(function()
    currentTab = "Chest"
    page1.Visible = false
    page2.Visible = true
    tab2.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
    tab2.TextColor3 = Color3.new(1, 1, 1)
    tab1.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    tab1.TextColor3 = Color3.fromRGB(150, 150, 150)
end)

-- LOGIC ĐÓNG / MỞ MENU BẰNG EFFECT CO GIÃN
close.MouseButton1Click:Connect(function()
    main:TweenSize(UDim2.fromOffset(0, 0), "In", "Back", 0.35, true, function()
        main.Visible = false
        OpenBtn.Visible = true
    end)
end)

OpenBtn.MouseButton1Click:Connect(function()
    OpenBtn.Visible = false
    main.Visible = true
    main:TweenSize(UDim2.fromOffset(420, 260), "Out", "Back", 0.4, true)
end)

-- LOGIC KÉO DI CHUYỂN MENU (DRAG ZONE) CHO MOBILE/PC
local dragToggle, dragStart, startPos
DragHeader.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragToggle = true
        dragStart = input.Position
        startPos = main.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragToggle = false end
        end)
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if dragToggle and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        main.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

--// LOGIC CHỨC NĂNG GAME GỐC (GIỮ NGUYÊN 100%)
local ESP = {}
local function createESP(plr)
    local box = Drawing.new("Square")
    box.Thickness = 1
    box.Filled = false
    box.Color = Color3.fromRGB(255,255,255)
    box.Visible = false

    local name = Drawing.new("Text")
    name.Size = 13
    name.Center = true
    name.Outline = true
    name.Color = Color3.fromRGB(255,255,255)
    name.Visible = false

    ESP[plr] = {box = box, name = name}
end

for _,p in ipairs(Players:GetPlayers()) do
    if p ~= LP then createESP(p) end
end
Players.PlayerAdded:Connect(function(p)
    if p ~= LP then createESP(p) end
end)

RunService.RenderStepped:Connect(function()
    if AutoHit then
        for _,plr in ipairs(Players:GetPlayers()) do
            if plr ~= LP and plr.Character and LP.Character then
                local hrp = plr.Character:FindFirstChild("HumanoidRootPart")
                local my = LP.Character:FindFirstChild("HumanoidRootPart")
                if hrp and my then
                    local dir = (hrp.Position - my.Position).Unit
                    RS.Events.GameRemoteFunction:InvokeServer(
                        "AttemptWeaponHit",
                        {
                            hitboxOffset=Vector3.new(0,0,-1.5),
                            knockback=50,
                            shouldLock=true,
                            shouldSlow=true,
                            attackCooldown=0,
                            hitboxSize=Vector3.new(9,14,8),
                            lungeKnockback=55,
                            slowTime=1.5,
                            slowMult=0.2,
                            cycleIndex=1,
                            damage=100,
                            tool=LP.Character:FindFirstChildOfClass("Tool"),
                            shouldLunge=true
                        },
                        {{
                            knockback=50,
                            isClosestEnemy=true,
                            origin=my.Position,
                            enemyModel=plr.Character,
                            distance=(hrp.Position-my.Position).Magnitude,
                            direction=dir
                        }}
                    )
                end
            end
        end
    end

    for plr,esp in pairs(ESP) do
        if PlayerESP then
            local char = plr.Character
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            local head = char and char:FindFirstChild("Head")

            if hrp and head then
                local top = Camera:WorldToViewportPoint(head.Position+Vector3.new(0,0.5,0))
                local bottom = Camera:WorldToViewportPoint(hrp.Position-Vector3.new(0,3,0))
                local h = math.abs(top.Y-bottom.Y)
                local w = h/2

                if top.Z>0 and bottom.Z>0 then
                    esp.box.Size = Vector2.new(w,h)
                    esp.box.Position = Vector2.new(top.X-w/2,top.Y)
                    esp.box.Visible = true
                    esp.name.Position = Vector2.new(top.X,top.Y-14)
                    esp.name.Text = plr.Name
                    esp.name.Visible = true
                else
                    esp.box.Visible=false
                    esp.name.Visible=false
                end
            end
        else
            esp.box.Visible=false
            esp.name.Visible=false
        end
    end

    if AutoBasic then
        RS.Events.GameRemoteFunction:InvokeServer("AttemptRollGachaChest","Basic")
    end
    if AutoDivine then
        RS.Events.GameRemoteFunction:InvokeServer("AttemptRollGachaChest","Divine")
    end
end)
